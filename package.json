{
  "name": "elm-parcel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cp -r ./src/assets ./dist && parcel ./src/index.html --out-dir dist",
    "build": "cp -r ./src/assets ./dist && parcel build ./src/index.html --out-dir dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-hot": "^1.1.4",
    "node-elm-compiler": "^5.0.4",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "elm-format": "^0.8.3",
    "elm-test": "^0.19.1-revision2"
  }
}
